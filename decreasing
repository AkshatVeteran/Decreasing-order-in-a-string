from collections import Counter
def most_frequent():
    test_str = "Missisipi"
    res = Counter(test_str)
    print("Count of all characters in Missisipi is :\n "
          + str(res))
f1=most_frequent()
